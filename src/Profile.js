import {Button} from "react-bootstrap";

const Profile = () => {
    return (
        <div>
            <h2>Profile Page</h2>
            <Button href="/">Home</Button>
        </div>
    )
}

export default Profile